### 列表容器-滚动懒加载数据-组件-IntersectionObserver API 实现方式 存在缺陷的问题说明

当列表快速滚动到中间某个位置时，这时候会加载渲染列表当前位置及前面的全部数据，而不是只加载当前视口可见区域的数据。


这个问题存在的原因：
当快速滚动时，IntersectionObserver 会把每一个滚动的元素都触发一次回调，如果只加载最后一次回调的内容，会导致只加载最后一张图片，视口范围内的其他图片都没有加载渲染。
