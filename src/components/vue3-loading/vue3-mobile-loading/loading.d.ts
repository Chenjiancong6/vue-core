import { ToastOptions } from 'vant';

export declare interface LoadingConfig {
  add: (toastOptions?: ToastOptions) => void,
  remove: () => void,
}

declare const _default: LoadingConfig;

export default _default;
